package ru.nsu.zarcer;

/**
 * Interface to add more polymorphism.
 */
interface PrimeFinderInterface {
    /**Method through all calculation goes.
     *
     * @param numbers array of int
     *
     * @return true if array has non-prime number, false otherwise
     */
    boolean checkNonPrime(int[] numbers);
}
